# OMC local state (never commit)
.omc/

# Delegate scratch / iteration snapshots
delegate-workspace/

# Secrets — credentials live in per-user env files, never committed
.env
**/.env
*.secret*
**/CREDENTIALS.md

# Per-user discovered config (generated by ulmo-ticket/discover.py)
.ulmo-ticket/

# Python / OS noise
__pycache__/
*.pyc
.DS_Store
node_modules/
skills/ulmo-ticket
ulmo-ticket-context/